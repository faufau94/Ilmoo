FROM node:20-alpine
RUN corepack enable
WORKDIR /app

RUN apk add --no-cache curl

COPY package.json yarn.lock ./
RUN yarn install --frozen-lockfile

COPY . .

EXPOSE 3000

# Generate migrations & start dev server
CMD ["yarn", "dev"]
